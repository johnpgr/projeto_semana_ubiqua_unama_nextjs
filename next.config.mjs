/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
            hostname: "www.essemundoenosso.com.br"
        }]
    }
};

export default nextConfig;
